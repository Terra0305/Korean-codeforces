services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - ALLOWED_HOSTS=localhost,127.0.0.1,backend,4.230.40.102
      - DEBUG=0
  frontend:
    build: ./frontend/app
    ports:
      - "80:80"
    depends_on:
      - backend
